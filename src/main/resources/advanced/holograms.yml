# HyXDuels is authoritative for every entry in this file. Runtime DH holograms are transient.
Enabled: false
# Applied by DecentHolograms; valid range is 20-72000 ticks.
Default-Update-Interval-Ticks: 100
# `/duel hologram create` adds validated entries here. Keep this section empty by default.
Managed: {}
