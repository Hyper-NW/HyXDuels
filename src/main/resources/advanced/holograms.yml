# Managed DecentHolograms leaderboards.
# HyXDuels owns these definitions; generated DecentHolograms objects are transient.
Config-Version: 1
Enabled: false

# Applied by DecentHolograms; valid range is 20-72000 ticks.
Default-Update-Interval-Ticks: 100

# `/duels hologram create` adds validated entries here. Keep this section empty by default.
Managed: {}
